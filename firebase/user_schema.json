{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "User",
  "type": "object",
  "properties": {
    "birthday": {
      "type": "string",
      "format": "date-time",
      "description": "The user's date of birth as a timestamp."
    },
    "created_time": {
      "type": "string",
      "format": "date-time",
      "description": "The time when the user account was created."
    },
    "display_name": {
      "type": "string",
      "description": "The display name chosen by the user."
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "The user's email address."
    },
    "ice_cream": {
      "type": "string",
      "description": "The user's favorite ice cream flavor."
    },
    "uid": {
      "type": "string",
      "description": "A unique identifier for the user."
    }
  },
  "required": ["created_time", "email", "uid"]
}
